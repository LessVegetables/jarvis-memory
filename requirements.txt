# Внешних зависимостей пока нет: шаги 1-4 работают на стандартной библиотеке.
#
# Появятся на шаге 5 (RAG):
#   sqlite-vec          — векторный поиск прямо в SQLite
#   onnxruntime         — запуск модели эмбеддингов на CPU платы
#   (модель: intfloat/multilingual-e5-small, 384 измерения, экспорт в ONNX)
#
# На шаге 6 (погода, 2GIS):
#   requests
