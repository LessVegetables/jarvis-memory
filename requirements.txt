# Steps 1-4 run on the standard library alone. Nothing to install yet.
#
# Step 5 (RAG), on the board:
#   sqlite-vec          163 KB   vector search inside SQLite (aarch64 wheel exists)
#   onnxruntime          21 MB   runs the embedding model on the CPU
#   tokenizers            ~3 MB  tokenizer only, the small Rust package
#   numpy                ~20 MB
#
# Step 8 (weather, 2GIS): nothing -- urllib from the standard library.
#
# Step 9 (calendar sync), on the board:
#   icalendar               parses .ics
#   recurring_ical_events   expands RRULEs (weekly lectures)
#   caldav                  iCloud only; pulls requests + lxml (~10 MB)
#
# NEVER install these on the board: torch, transformers, sentence-transformers.
# They are multi-gigabyte on ARM64 -- a fifth of the 15 GB disk -- and are only
# needed to EXPORT the model, which happens on a laptop via
# tools/export_embedding_model.py. The board loads a .onnx file instead.
