# No external dependencies yet: steps 1-4 run on the standard library.
#
# Arriving at step 5 (RAG):
#   sqlite-vec          - vector search inside SQLite itself
#   onnxruntime         - runs the embedding model on the board's CPU
#   (model: intfloat/multilingual-e5-small, 384 dimensions, exported to ONNX)
#
# At step 6 (weather, 2GIS):
#   requests
